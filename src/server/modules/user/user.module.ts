import { Module } from '@hono-di/core';

@Module({
  imports: [
    // hono-di:imports
  ],
  controllers: [
    // hono-di:controllers
  ],
  providers: [
    // hono-di:providers
  ],
})
export class UserModule {}
